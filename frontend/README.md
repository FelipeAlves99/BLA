# TaskFlow Frontend

Reserved for the React + TypeScript application. The frontend will authenticate with Keycloak using Authorization Code Flow with PKCE and call the backend with a bearer access token.
